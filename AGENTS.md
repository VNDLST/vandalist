See [CLAUDE.md](CLAUDE.md) — that's the canonical/full version of this file.

(This used to be the other way around, with `CLAUDE.md` as a symlink to
this file. Swapped 2026-08-07 because this repo lives inside a
SharePoint/OneDrive-synced folder that flattens symlinks, and `CLAUDE.md`
specifically needs to always have real content since Claude Code loads it
automatically. Keep this file as a short pointer, not a second full copy —
duplicating the content here would drift out of sync with `CLAUDE.md` over
time.)
