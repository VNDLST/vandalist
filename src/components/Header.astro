---
const links = [
  { label: "Services", href: "/services" },
  { label: "Work", href: "/work" },
  { label: "Case Studies", href: "/case-studies" },
  { label: "How We Work", href: "/how-we-work" },
  { label: "About", href: "/about" },
  { label: "Contact", href: "/contact" },
];
const currentPath = Astro.url.pathname.replace(/\/$/, "") || "/";
---
<header class="w-full py-8 md:py-10">
  <nav class="max-w-7xl mx-auto px-6 md:px-10 flex items-center justify-between">
    <a href="/" class="shrink-0">
      <img src={`${import.meta.env.BASE_URL}/vandalist-logo.png`} alt="Vandalist" class="h-10 md:h-12 w-auto" />
    </a>

    <ul class="hidden md:flex items-center gap-8 text-sm text-[var(--color-slate)]">
      {links.map((link) => (
        <li>
          <a
            href={link.href}
            class:list={[
              "transition-colors",
              currentPath === link.href ? "text-[var(--color-accent)] font-medium" : "hover:text-[var(--color-ink)]",
            ]}
          >
            {link.label}
          </a>
        </li>
      ))}
    </ul>

    <!-- mobile: simple stacked link row, no hamburger complexity for v1 -->
    <ul class="flex md:hidden items-center gap-3 text-xs text-[var(--color-slate)] overflow-x-auto">
      {links.map((link) => (
        <li class="shrink-0">
          <a
            href={link.href}
            class:list={currentPath === link.href ? "text-[var(--color-accent)] font-medium" : ""}
          >
            {link.label}
          </a>
        </li>
      ))}
    </ul>
  </nav>
</header>
